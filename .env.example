# S7-003 local MLflow registry shape only; do not commit real values in .env files.
# Copy to an ignored .env file or export these variables in your shell before using
# `docker compose --profile core --profile research ...`.
MLFLOW_POSTGRES_DB=
MLFLOW_POSTGRES_USER=
MLFLOW_POSTGRES_PASSWORD=
MLFLOW_MINIO_ROOT_USER=
MLFLOW_MINIO_ROOT_PASSWORD=
