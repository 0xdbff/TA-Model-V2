# Master Loop Status — S7 Model Candidate and Registry

**Loop UUID:** `e97718d3-0a6c-4322-8e08-9426a1622e12`  
**Loop branch:** `agent/loop-e97718d3-0a6c-4322-8e08-9426a1622e12`  
**Loop worktree:** `/Users/db/dev/TA-Model-v2/TA-Model-V2-loop-e97718d3-0a6c-4322-8e08-9426a1622e12`  
**Base:** latest `origin/dev` at `b554eb7`  
**Final PR base:** `dev`  
**Final PR source:** `agent/loop-e97718d3-0a6c-4322-8e08-9426a1622e12`  
**Final PR:** pending

## Scope and sprint objective

Sprint IDs in scope: `S7-*`  
GitHub issues in scope: `#31`-`#34`

Sprint objective: deliver the first validation-first probabilistic model candidate path: a reproducible training runner, probabilistic forecast outputs with uncertainty/quantiles, MLflow registry metadata with rollback controls, and a model-gate report that compares against fixed S5 baselines without enabling paper/live routing or autonomous promotion.

## Binding controls

- `docs/rules/00_delivery_anti_drift_rules.md`
- `docs/02_requirements_catalog.csv` (`FR-008`, `FR-014`, `FR-019`, `NFR-005`; `FR-007` as baseline prerequisite)
- `docs/10_implementation_roadmap.md` Sprint 7
- `docs/11_tech_stack_and_docker.md` MLflow/PyTorch/Hugging Face/Docker guardrails
- `docs/13_github_issue_backlog.md` S7 entries

Hard guardrails: MVP remains validated paper trading only; spot/liquid instruments only; no live capital, leverage, derivatives, margin, shorting, market making, HFT, autonomous model promotion, online self-update, unapproved data, unapproved runtime/datastore/broker additions, risk/kill-switch bypass, event-time leakage, or use of gross-only/baseline-free model evidence. Candidate models must remain gated and must not directly feed paper/live routes.

## Issue status

| Issue | Sprint | Status at loop start | Evidence / notes |
|---|---|---:|---|
| #31 | S7-001 | PR #96 merged to loop | Reproducible training-run contracts and runner added. Run ID binds dataset snapshot/hash, config, SHA-like code commit, seed, metrics, artifacts, and non-promotable runner status. |
| #32 | S7-002 | PR #97 merged to loop | Forecast and probabilistic candidate contracts added with probabilities, quantiles, uncertainty, calibration metadata, dataset/run/model lineage, lineage-tamper guards, and deterministic sequence-conditioned train-only fitting. |
| #33 | S7-003 | PR #98 merged to loop | MLflow registry metadata contracts and core/research Compose config added with candidate/champion/shadow/rejected/archive states, rollback/review/approval controls, lineage-tamper guards, localhost-bound ports, required external env vars, non-root MLflow image, and healthcheck. |
| #34 | S7-004 | Open | Needs calibration/uncertainty/ablation report with NLL, Brier, quantile coverage, calibration, and baseline comparison. |

## Dependencies and sequencing

1. `#31` / `S7-001` is foundational: training config/run-result contracts and deterministic fixture runner must land before candidate/report work can depend on stable run IDs.
2. `#32` / `S7-002` depends on `#31` and should add forecast/model contracts plus a deliberately small deterministic candidate before any larger transformer/Hugging Face work.
3. `#33` / `S7-003` depends on `#31` contract shape and must stay on the approved MLflow/PostgreSQL/MinIO path; it may be developed after `#31` branch creation but should integrate after runner metadata is stable.
4. `#34` / `S7-004` is validation-last and must consume `#31` run IDs, `#32` forecast outputs, `#33` registry metadata, and fixed S5 baseline evidence.
5. Final loop validation must exercise the lowest-risk integrated path: fixture dataset snapshot -> training runner -> candidate forecast outputs -> registry metadata -> model-gate report.

## Sub-agent assignments

| Issue | Sprint | Branch | Worktree | Agent | PR | Status |
|---|---|---|---|---|---|---|
| #31 | S7-001 | `agent/31-S7-001` | `/Users/db/dev/TA-Model-v2/TA-Model-V2-31-S7-001-e97718d3-0a6c-4322-8e08-9426a1622e12` | backend-impl | [#96](https://github.com/0xdbff/TA-Model-V2/pull/96) merged | Completed after QA-requested SHA-like commit validation and out-of-sample evaluation-split guard. |
| #32 | S7-002 | `agent/32-S7-002` | `/Users/db/dev/TA-Model-v2/TA-Model-V2-32-S7-002-e97718d3-0a6c-4322-8e08-9426a1622e12` | backend-impl | [#97](https://github.com/0xdbff/TA-Model-V2/pull/97) merged | Completed after QA-requested lineage-hash hardening and upgrade from train-prior-only to sequence-conditioned candidate behavior. |
| #33 | S7-003 | `agent/33-S7-003` | `/Users/db/dev/TA-Model-v2/TA-Model-V2-33-S7-003-e97718d3-0a6c-4322-8e08-9426a1622e12` | infra-impl | [#98](https://github.com/0xdbff/TA-Model-V2/pull/98) merged | Completed after QA/security-requested lineage guards, no default secrets, localhost-bound registry ports, non-root MLflow image, healthcheck, and policy tests. |
| #34 | S7-004 | `agent/34-S7-004` | `/Users/db/dev/TA-Model-v2/TA-Model-V2-34-S7-004-e97718d3-0a6c-4322-8e08-9426a1622e12` | backend-impl / QA | pending | Planned last; validation/reporting consumer of #31-#33. |

## Validation plan

Required per implementation PR unless a narrower first pass is explicitly justified:

```text
uv run ruff check .
uv run mypy src tests
uv run pytest
```

Focused prerequisite and S7 checks:

```text
uv run pytest tests/test_dataset_snapshot_builder.py tests/test_s4_leakage_parity.py tests/test_deterministic_baselines.py tests/test_evaluation_scorecard.py
uv run pytest tests/test_*model* tests/test_*training* tests/test_*registry* tests/test_*calibration*
```

Docker/runtime checks expected for `#33` and final integration if Compose changes land:

```text
docker compose config
docker compose --profile core --profile research config
```

Full service startup is required only if the implemented Compose services and local environment can support it safely without secrets or live credentials.

## QA findings and integration risks

- Current `origin/dev` has no `src/ta_model/models`, `training`, or `registry` modules; S7 must create reusable contracts rather than ad hoc scripts.
- Current dependencies do not include MLflow, PyTorch, scikit-learn, or LightGBM. Any addition must be pinned, reflected in `uv.lock`, and documented with Docker/profile impact.
- Current Compose has only `stream` profile Redpanda services. MLflow registry work must use the approved PostgreSQL/MinIO/MLflow/core/research stack or stop for ADR/human sync.
- Lookahead leakage is the primary stop-the-line risk: training transforms, normalization, labels, ablations, and calibration must respect chronological split boundaries and event-time availability.
- Model evidence must not become gross-only or predictor-only. Reports must include probabilistic metrics, calibration, uncertainty/quantile coverage, and baseline comparison.
- No auto-promotion: registry state transitions must remain gated, auditable, and owner-reviewed; rollback metadata is mandatory for candidate/champion records.
- Hugging Face/large checkpoint use is not needed for the first candidate. If introduced, it must obey revision/license/offline artifact/token guardrails and must not add a new runtime path.

## Integrated validation log

- Loop worktree created from latest `origin/dev` at `b554eb7`.
- S7 issue inventory reviewed: open issues #31-#34 under milestone `S7 Model candidate and registry`.
- Read-only exploration mapped existing dataset/baseline/evaluation prerequisites and confirmed S7 implementation gaps.
- #31 worktree created from `agent/loop-e97718d3-0a6c-4322-8e08-9426a1622e12` at loop commit `8d24e69`.
- #31 PR #96 pre-merge validation in sub-agent worktree after QA fixes: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 222 tests.
- Independent QA review approved #31 with non-blocking handoff note: artifact references are not storage-backed until #33 registry/storage work.
- Loop validation after #31 merge: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 222 tests.
- #32/#33 worktrees created from latest loop commit `a6f07ae` after #31 integration.
- #32 PR #97 initial review blocked train-prior-only behavior and lineage-hash gaps. Sub-agent fixed with sequence-conditioned scoring, forecast/output lineage hash binding, candidate-name-bound model version hash, and regression tests.
- #32 PR #97 pre-merge validation in sub-agent worktree after QA fixes: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 230 tests.
- Loop validation after #32 merge: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 230 tests.
- #33 PR #98 initial QA/security reviews blocked malformed registry lineage acceptance, committed credential defaults, broad registry port exposure, root MLflow image, and missing MLflow healthcheck. Sub-agent fixed with lineage guards, required external env vars, localhost port binding, non-root image, healthcheck, governance hardening, and policy tests.
- #33 PR #98 pre-merge validation in sub-agent worktree after QA/security fixes: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 240 tests; `docker compose config` and `docker compose --profile core --profile research config` passed with ephemeral env vars supplied outside git.
- Loop validation after #33 merge: `uv run ruff check .` passed; `uv run mypy src tests` passed; `uv run pytest` passed with 248 tests; Docker Compose config checks for default/core/research profiles passed with ephemeral env vars supplied outside git.

## Human-sync decisions

None yet.

## Remaining blockers

- No implementation blockers yet.
- #34 sub-agent worktree and branch still need to be created from the latest loop branch before assignment.
